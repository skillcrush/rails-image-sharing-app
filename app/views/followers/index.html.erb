<div class="header">
  <h1>
  <i class="fa fa-users"></i>
    The people you follow:</h1>
</div>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @followed.each do |followed| %>
      <tr>
        <td><%= "#{followed.first_name} #{followed.last_name}" %></td>
       <!-- <td><%= link_to 'Boards', followed %></td>
        <td><%= link_to 'Unfollow', followed, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Follower', new_follower_path %>

